name: On Merge

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

concurrency:
  group: publish-image-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    uses: meyersa/iac-ci/.github/workflows/reusable-on-merge.yml@main
    secrets: inherit